apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: sergio-guest-template-test-20260831
  title: Guest Template Validation
  description: Harmless no-op security validation template
spec:
  owner: group:default/guests
  type: service
  parameters: []
  steps:
    - id: harmless-log
      name: Harmless validation step
      action: debug:log
      input:
        message: Guest template registration validation only
